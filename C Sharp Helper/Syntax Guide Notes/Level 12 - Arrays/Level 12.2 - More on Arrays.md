# A Complex Array
The following is a much more complex array example. 
Here we start by setting the max value that is has to be larger than the array. 

In this case we have a value for `currentSmallest` which we are 